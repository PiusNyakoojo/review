/*
    The terminal in webstorm is not unix based, it's based on command prompt (cmd.exe)

    go get -u <some-package>

    -u
        this means update
    
    Generally when working with unix commands a single "-" means the following is a single letter. 
    "--" means that a word is following.
*/