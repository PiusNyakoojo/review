/*
    We can initialize variabels right before our expression in an if statement

    // the scope of the variable food is within that if block
    
    if food := "Veggie burger"; true {
        fmt.Println(food)
    }

    if err := file.Chmod(0664); err != nil {
        log.Print(err)
        return err
    }
*/